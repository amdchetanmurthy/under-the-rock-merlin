description: "MPIFoE nightly tests - sanity + bringup + functional + state-machine + error-handling + driver + RAS (excludes stress and interaction)"

test_cases:
  - sanity/*
  - bringup/*
  - functional/*
  - state-machine/*
  - error-handling/*
  - driver/*
  - ras/*
